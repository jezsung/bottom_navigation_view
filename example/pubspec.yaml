name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  bottom_navigation_view:
    path: ../
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
