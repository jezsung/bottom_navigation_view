name: bottom_navigation_builder
description: A set of widgets that helps you to implement bottom navigation.
version: 0.1.1
homepage: https://github.com/jezsung/bottom_navigation_builder

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
